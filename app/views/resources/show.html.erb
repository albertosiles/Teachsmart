
<!-- Display one image in the view --->
<%#= cl_image_tag @resource.photo.key, height: 300, width: 400, crop: :fill %>

 <!-- Display multiple images in the view. Needs to be placed in the desired position  --->
    <%# <div class="uploaded-photo">
      <% @resource.photos.each do |photo| %>
        <%#= cl_image_tag photo.key, height: 50, width: 80, crop: :fill %>
      <%# end %>
    <%# </div>  %>

<!--As a background image:  -->
<%# <div class="card-category" %>
  <%# style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @article.photo.key, height: 300, crop: :fill')">  %>
<%# </div> %>


<!-- Display one file in the view --->
<%#= cl_image_tag @resource.file.key, height: 300, width: 400, crop: :fill %>

<main>
  <div class="d-flex justify-content-between align-items-top">
    <h1 class="title-resource-class"><%= @resource.title %></h1>
    <br>
  </div>
  <br>
  <div class="d-flex justify-content-between">
    <div class="d-flex flex-column">
      <div class="circle">
        <div class="circle-text">Category: <h6><%= @resource.category %></h6></div>
      </div>
      <div class="circle">
        <div class="circle-text">Category: <h6><%= @resource.category %></h6></div>
      </div>
    </div>
    <div>
      <div class="description-pdf-container">
        <div class="resource-description">
          <%= @resource.description %>
        </div><br>
        <div class="uploaded-pdf">
          <!-- Display multiple files in the view. Needs to be placed in the desired position --->
            <% @resource.files.each do |file| %>
              <iframe src= <%= file.key %> width="600px" frameborder="0" loading= "lazy"></iframe>
            <% end %>
        </div>
      </div>
    </div>
    <div class="d-flex flex-column">
      <div class="circle">
        <div class="circle-text">Category: <h6><%= @resource.category %></h6></div>
      </div>
      <div class="circle">
        <div class="circle-text">Category: <h6><%= @resource.category %></h6></div>
      </div>
    </div>
  </div>

  <br>
  <br>
  <div class="d-flex justify-content-between">
    <% if current_user.bookmarks.exists?(resource_id: @resource.id) %>
      <%= button_to 'Remove from Bookmarks', bookmark_path(current_user.bookmarks.find_by(resource_id: @resource.id)), method: :delete, data: { confirm: 'Are you sure you want to remove this bookmark?' }, class: "btn btn-secondary" %>
    <% else %>
      <%= button_to 'Bookmark', bookmark_resource_path(@resource), method: :post, class: "btn btn-secondary" %>
    <% end %>
    <button class="btn btn-secondary">Download</button>
  </div>
  <p class="plain-links"><%= link_to 'Back to teaching materials', resources_path %></p>

  <% if @resource.user == current_user %>
    <p class="plain-links"><%= link_to 'Edit', edit_resource_path(@resource) %></p>
    <p class="plain-links"><%= link_to 'Delete', resource_path(@resource), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
  <% end %>
  <br>
  <br>
  <h4 class="title-review-class">What other teachers thought</h4>

  <ul class="review-list">
    <% @resource.reviews.each do |review| %>
      <li>
        <% review.rating.times do %>
          <i class="fa-solid fa-star"></i>
        <% end %>

              <em class="review-content"><%= review.content %></em></li>
    <% end %>
  </ul>
</main>
